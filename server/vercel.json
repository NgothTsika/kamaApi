{
  "buildCommand": "prisma generate && yarn build",
  "functions": {
    "server/api/index.ts": {
      "runtime": "@vercel/node",
      "memory": 1024,
      "maxDuration": 60
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/api/index.ts"
    }
  ]
}
