{
  "buildCommand": "npx prisma generate && yarn build",
  "functions": {
    "server/dist/index.ts": {
      "maxDuration": 60
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/dist/index.ts"
    }
  ]
}
