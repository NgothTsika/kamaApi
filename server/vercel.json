{
  "buildCommand": "npx prisma generate && yarn build",
  "functions": {
    "dist/api/index.js": {
      "maxDuration": 60,
      "runtime": "nodejs20.x"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "dist/api/index.js"
    }
  ]
}
