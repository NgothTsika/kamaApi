{
  "buildCommand": "cd server && npx prisma generate && npm run build",
  "functions": {
    "server/dist/api/index.js": {
      "maxDuration": 60
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "server/dist/api/index.js"
    }
  ]
}
