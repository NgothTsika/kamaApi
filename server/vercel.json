{
  "buildCommand": "npx prisma generate && yarn build",
  "functions": {
    "server/api/index.ts": {
      "maxDuration": 60
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/server/api/index.ts"
    }
  ]
}
