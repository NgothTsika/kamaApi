{
  "buildCommand": "npx prisma generate && yarn build",
  "functions": {
    "dist/api/index.js": {
      "maxDuration": 60
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "dist/api/index.js"
    }
  ]
}
